# 📖 记账本 - 使用说明

## 一、快速开始

### 1. 在手机上打开
将 `index.html` 文件发送到手机（微信/邮件/云盘均可），用手机浏览器打开。

### 2. 添加到主屏幕（变成 App）
- **iPhone (Safari)**：点击底部「分享」按钮 → 选择「添加到主屏幕」
- **Android (Chrome)**：点击右上角「⋮」菜单 → 选择「添加到主屏幕」

添加后，桌面会出现记账本图标，点击即可全屏使用，和原生 App 体验一致。

### 3. 开始记账
点击右下角绿色 **「+」** 按钮 → 选择类型(支出/收入) → 输入金额 → 选择分类 → 选择支出标签 → 保存。

---

## 二、功能说明

| 功能 | 说明 |
|------|------|
| 📊 **月度概览** | 首页顶部显示当月收入、支出、结余总额 |
| 🥧 **分类饼图** | 按支出分类统计，展示各分类的金额和占比 |
| 🏷️ **标签饼图** | 按支出标签统计，查看「生存必要支出」和「浮动弹性支出」的占比 |
| 📝 **收支明细** | 按日期倒序排列所有记录，点击可编辑或删除 |
| 📅 **月份切换** | 左右箭头切换查看不同月份的数据 |
| 🏷️ **分类管理** | 支出/收入分类均可自定义新增和删除 |
| 🏷️ **标签管理** | 支出标签可自定义新增和删除 |
| 🎯 **存钱计划** | 新增存钱计划，查看全部/进行中/已完成数量，存入金额，标记完成 |
| 📤 **数据导出** | 设置页可导出 JSON 备份文件 |
| 📥 **数据导入** | 从备份文件恢复数据（换手机时用） |

---

## 三、默认分类与标签

### 支出分类
🍚 餐饮、🛍️ 购物、🎮 娱乐、📚 提升、🚌 交通、📦 其它

### 收入分类
💰 工资、💼 兼职、📈 理财、✨ 其它

### 支出标签
- **生存必要支出**：房租、水电、餐饮、交通等必须开销
- **浮动弹性支出**：购物、娱乐、旅游等非必须开销

> 💡 分类和标签都可以点击「自定义」按钮新增自己的分类/标签。默认的分类/标签不能删除，自定义的可以删除（前提是没有记录在使用）。

---

## 四、数据安全

- 所有数据存储在**手机浏览器本地**（localStorage），不会上传到任何服务器
- 建议定期使用「导出数据」功能备份
- 清除浏览器缓存会导致数据丢失，请提前导出备份

---

## 五、常见问题

**Q：换手机后数据怎么办？**
A：在旧手机上导出数据 → 将 JSON 文件传到新手机 → 在新手机打开记账本 → 设置页导入数据。

**Q：数据会丢失吗？**
A：正常使用不会丢失。但如果清除浏览器数据或卸载浏览器，数据会丢失。请定期导出备份。

**Q：可以多设备同步吗？**
A：当前版本为本地存储，暂不支持自动同步。可通过导出/导入手动迁移数据。

**Q：收入和支出都能自定义分类吗？**
A：是的。在添加记录时，支出和收入分类下都有「自定义」按钮，可以分别添加。

**Q：标签有什么用？**
A：标签用于区分支出的性质。比如你可以把餐饮、交通标记为「生存必要支出」，把购物、娱乐标记为「浮动弹性支出」。首页饼图可以切换「按分类」或「按标签」查看支出结构。

---

## 六、版本历史

**v2.0.0**
- 新增支出标签功能（生存必要支出、浮动弹性支出，可自定义）
- 新增按标签统计的饼图
- 新增自定义支出/收入分类
- 新增存钱计划功能
- 优化底部导航图标

**v1.0.0**
- 初始版本：记账、月度汇总、分类饼图、明细列表、数据导入导出
